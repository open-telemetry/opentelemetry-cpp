{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug on Linux",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceFolder}/bazel-bin/<path to the bin file>",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}
