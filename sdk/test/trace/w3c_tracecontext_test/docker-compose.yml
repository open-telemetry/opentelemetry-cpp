version: "3" 

services:
  driver:
    build: driver
    environment:
      - SERVER_URI=http://server:30000/test
    depends_on: 
      - app

  app:
    build: app
    volumes:
      - ../../../../.:/opentelemetry-cpp
