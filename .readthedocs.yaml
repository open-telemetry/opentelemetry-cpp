version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - doxygen
    - graphviz

sphinx:
  configuration: docs/public/conf.py

python:
  install:
    - requirements: docs/public/requirements.txt
