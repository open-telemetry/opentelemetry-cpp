<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.9.8" xml:lang="en-US">
  <compounddef id="HEADER_ONLY_SINGLETON" kind="page">
    <compoundname>HEADER_ONLY_SINGLETON</compoundname>
    <title>Header only singleton.</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<sect1 id="HEADER_ONLY_SINGLETON_1ELF_SINGLETON">
<title>ELF_SINGLETON</title>
<para>For clang and gcc, the desired coding pattern is as follows.</para>
<para><verbatim>  class Foo
  {
    // (a)
    __attribute__((visibility(&quot;default&quot;)))
    // (b)
    T&amp; get_singleton()
    {
      // (c)
      static T singleton;
      return singleton;
    }
  };</verbatim></para>
<para>(a) is needed when the code is build with <programlisting><codeline><highlight class="normal">-fvisibility=</highlight><highlight class="stringliteral">&quot;hidden&quot;</highlight><highlight class="normal"><sp/></highlight></codeline>
</programlisting> to ensure that all instances of (b) are visible to the linker.</para>
<para>What is duplicated in the binary is <emphasis>code</emphasis>, in (b).</para>
<para>The linker will make sure only one instance of all the (b) methods is used.</para>
<para>(c) is a singleton implemented inside a method.</para>
<para>This is very desirable, because:</para>
<para><itemizedlist>
<listitem><para>the C++ compiler guarantees that construction of the variable (c) is thread safe.</para>
</listitem><listitem><para>constructors for (c) singletons are executed in code path order, or not at all if the singleton is never used.</para>
</listitem></itemizedlist>
</para>
</sect1>
<sect1 id="HEADER_ONLY_SINGLETON_1OTHER_SINGLETON">
<title>OTHER_SINGLETON</title>
<para>For other platforms, header only singletons are not supported at this point.</para>
</sect1>
<sect1 id="HEADER_ONLY_SINGLETON_1CODING_PATTERN">
<title>CODING_PATTERN</title>
<para>The coding pattern to use in the source code is as follows</para>
<para><verbatim>  class Foo
  {
    OPENTELEMETRY_API_SINGLETON
    T&amp; get_singleton()
    {
      static T singleton;
      return singleton;
    }
  };</verbatim> </para>
</sect1>
    </detaileddescription>
    <location file="/home/ubuntu/opentelemetry-cpp/api/include/opentelemetry/common/macros.h"/>
  </compounddef>
</doxygen>
